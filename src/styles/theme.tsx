import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  spacing: 4,
  palette: {
    primary: {
      main: '#25c2a0',
    },
    secondary: {
      main: '#ffffff',
    },
    text: {
      primary: '#000000',
      secondary: '#ffffff',
    },
  },
});

export default theme;
